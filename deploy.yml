name: 🚀 Deploy website to DirectAdmin via FTP

on:
  push:
    branches:
      - main   # hoặc 'develop' nếu bạn deploy branch develop
  workflow_dispatch:  # cho phép chạy thủ công

jobs:
  ftp-deploy:
    name: Upload via FTP
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Upload to FTP server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./
          server-dir: /domains/trangsucse0001ueh.id.vn/public_html/
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
